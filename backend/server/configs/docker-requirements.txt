# Docker-specific requirements for the AUSTIN-Lang transcription server.
# This file is intentionally minimal — it only lists what the server code
# directly imports so the image stays lean and cross-platform.

# Web framework
fastapi==0.115.14
uvicorn[standard]==0.32.1
pydantic==2.12.5
python-multipart==0.0.18
starlette==0.46.2
aiofiles==23.2.1

# Transcription — Whisper
openai-whisper==20240930
tiktoken==0.12.0

# PyTorch (CPU wheels by default; override with a CUDA base image for GPU)
torch==2.10.0
torchaudio==2.10.0

# Numerical / audio helpers pulled in by Whisper
numpy==2.2.6
scipy==1.15.3
librosa==0.10.2.post1
soundfile==0.13.1
ffmpy==1.0.0

# Misc
pyyaml==6.0.3
requests==2.32.5
tqdm==4.67.2
