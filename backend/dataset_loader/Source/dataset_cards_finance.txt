# One Hugging Face dataset card per line.
# Blank lines and lines starting with # are ignored.
# To pin a specific config, append ":<config>" (e.g. org/name:L).
# To pin a specific split, append "@<split>" (e.g. org/name:L@test).
# Missing config/split falls back to the first advertised by HF.

kensho/spgispeech:S
distil-whisper/earnings22:chunked@test
